# 性能测试方案

## 1. 文档控制

| 项目 | 内容 |
|---|---|
| 文档编号 | TMS-PT-001 |
| 被测版本 | Git commit `0f07404` |
| 编制日期 | 2026-06-15 |
| 状态 | 方案已编制，执行待排期 |

## 2. 测试目标

验证系统在预期并发负载下的吞吐量、响应时间和稳定性，发现性能瓶颈和资源泄漏。

## 3. 测试场景

### FT-034：100 用户并发下单

| 项目 | 内容 |
|---|---|
| 场景描述 | 100 个顾客同时向同一商家下单 |
| 工具 | Python `concurrent.futures.ThreadPoolExecutor` + `httpx` |
| 并发数 | 100 |
| 通过标准 | 响应时间 P95 < 500ms，无 5xx 错误 |
| 数据准备 | 注册 100 个 customer 账户，商家菜品已上架 |
| 预期响应 | 全部返回 200，订单数据一致（无丢失/重复） |

```python
# 并发下单脚本框架
import time, statistics
from concurrent.futures import ThreadPoolExecutor, as_completed
from httpx import Client

def place_order(customer_token):
    with Client(base_url="http://127.0.0.1:8001") as c:
        start = time.perf_counter()
        resp = c.post("/api/customer/order", headers=..., json=...)
        elapsed = time.perf_counter() - start
        return resp.status_code, elapsed

with ThreadPoolExecutor(max_workers=100) as pool:
    futures = [pool.submit(place_order, tok) for tok in tokens]
    results = [f.result() for f in as_completed(futures)]

latencies = [r[1] for r in results]
print(f"成功率: {sum(1 for r in results if r[0]==200)}/100")
print(f"P50: {statistics.median(latencies)*1000:.1f}ms")
print(f"P95: {sorted(latencies)[int(len(latencies)*0.95)]*1000:.1f}ms")
print(f"P99: {sorted(latencies)[int(len(latencies)*0.99)]*1000:.1f}ms")
```

### FT-035：持续压力测试

| 项目 | 内容 |
|---|---|
| 场景描述 | 持续 5 分钟高频 API 请求（混合读/写/WS） |
| 工具 | `locust` 或 Python 自研 |
| 请求速率 | 50 req/s 起步，逐步升至 200 req/s |
| 通过标准 | 错误率 < 1%，无崩溃，无内存持续增长 |

### FT-036：WebSocket 推送延迟

| 项目 | 内容 |
|---|---|
| 场景描述 | 测量下单→商家收到 WebSocket 推送的端到端延迟 |
| 工具 | Python `asyncio` + `websockets` |
| 样本量 | 100 次 |
| 通过标准 | P95 < 1 秒 |
| 测量方法 | 客户端记录 `client_send_time`，商家 WS 收到后回传 timestamp，计算差值 |

```python
# WebSocket 延迟测试脚本框架
import asyncio, time, statistics
import websockets, httpx, json

async def measure_latency():
    # 1. 商家连接 WS
    # 2. 顾客下单（记录 send_time）
    # 3. 商家收到 new_order 推送（记录 recv_time）
    # 4. latency = recv_time - send_time
    pass

async def main():
    latencies = []
    for _ in range(100):
        lat = await measure_latency()
        latencies.append(lat)
        await asyncio.sleep(0.5)  # 间隔
    
    print(f"样本: {len(latencies)}, P50: {statistics.median(latencies)*1000:.0f}ms, P95: {sorted(latencies)[95]*1000:.0f}ms")
```

## 4. 环境要求

| 项目 | 配置 |
|---|---|
| OS | Windows 11 / Linux |
| CPU | 4 核+ |
| 内存 | 8 GB+ |
| 数据库 | MySQL 8.0（生产等效环境） |
| 后端 | `uvicorn` 多 worker（`--workers 4`） |

## 5. 残余风险

- 当前性能测试方案已完成编制，待具备测试环境后执行。
- SQLite 锁机制与 MySQL 不同，并发结论需在 MySQL 上验证。
- 以上测试 FT-034~FT-036 不作为当前版本发布阻断项。
